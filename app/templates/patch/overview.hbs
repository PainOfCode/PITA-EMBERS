{{#if session.isAuthenticated}}
<button class="patch_btns1" {{action "gotoNew"}}>Create new Patch-Note</button>
{{/if}}
<div>
    <div class="patch_ovw">
      {{#each model as |patch|}}
        {{patch-list patch=patch}}
      {{/each}}
      {{outlet}}
</div>
</div>
